const cors = require("cors");
// app.use(
//   cors({
//     origin: "http://localhost:3000",
//     methods: ["GET", "POST", "DELETE", "PUT", "OPTIONS"],
//     credentials: true,
//   })
// );

//app.listen(5432) Para Render.

const db = new Sequelize("housedb", "brandon", process.env.DB_PASSWORD, {
  host: process.env.DB_HOST,
  dialect: "postgres",
  logging: false,
  timezone: "-03:00",
});
